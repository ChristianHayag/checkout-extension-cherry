query RunInput {
  cart {
    lines {
      id
      quantity
      cost {
        amountPerQuantity {
          amount
        }
      }
      prices: attribute(key: "_prices") {
        value
      }
    }
  }
}